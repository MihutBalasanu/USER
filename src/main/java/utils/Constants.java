/**
 * Package utils contains utility tools used in the other packages: constants, costumed exception,
 * general method to read from a file, menus of the application.
 */
package utils;

/**
 * Holds the constants used by the application.
 */
public class Constants {

    /**
     * Path to the file in which there are all the usernames and passwords that are recognized as valid by the
     * application.
     */
    public static final String USER_FILE_PATH = "src/main/resources/users.txt";

    /**
     * Path to the file where application read and write the bank accounts created by the users.
     */
    public static final String ACCOUNT_FILE_PATH = "src/main/resources/account details.txt";
}
